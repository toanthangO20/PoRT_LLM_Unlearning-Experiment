# Recreated PoRT Artifact Bootstrap v1

This branch stores split chunks for `paper_port_recreated_artifacts_bootstrap.zip`.
It is intentionally separate from `master` so normal Kaggle repo clones stay small.
